import React from 'react';
import { Spin } from 'antd';

const Spinner = () => {
  return (
    <Spin
      size="large"
      style={{
        margin: 40,
      }}
    />
  );
};
export default Spinner;
